git clone [repository]
git status
git add
git commit -m "message/comment about commit submitting"
git commit --dry-run - tests commit prior to submitting
git push "adds the commits to the repo"
git pull "pulls down other submission"

escape : wc "saves and exits out of vi"

git remote -v ; after cloning check to see correct path of fetch/push
